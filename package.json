{
  "name": "vue-tanstack-datagrid",
  "private": true,
  "workspaces": [
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "dev:frontend": "bun run --cwd frontend dev",
    "build:frontend": "bun run --cwd frontend build",
    "type-check:frontend": "bun run --cwd frontend type-check",
    "build:datagrid": "bun run --cwd packages/datagrid build",
    "type-check:datagrid": "bun run --cwd packages/datagrid type-check"
  }
}
